users:
  www-data:
    groups:
      - www-data
    home: /var/www
  root:
    groups:
      - root
    home: /root
    authorized_keys:
      - thoas.pub

pyenv:
  path: "/var/www/pyenv"
  owner: "www-data"
  python_version: "3.6.3"
